@model List<ArticleView>
@{
    ViewBag.Title = "Formation";
}
<body id="js-formation" class="formation">
    <div class="wrapper">
        @Html.Partial("Navigation")
        @Html.Partial("Axes/AxeFormation")
        <div class="grid-2 grid-article">
            @foreach (var article in Model)
            {
                @Html.Partial("Articles/ArticleResumeFormation", article)
            }
        </div>