using Apropos.Domain;
using Apropos.Test.Core;
using Microsoft.Extensions.Logging;
using System.Collections.Generic;
using Xunit;

namespace Apropos.Test.Domain
{
    public class ArticleServiceTest
    {
        [Fact]
        public void GetArticlesTest()
        {
            //ILogger logger = new LoggerStub();
            //List<Article> articles = new ArticleService(null, logger).GetArticles("_data\\articles");

            //Assert.Equal(4, articles.Count);
            //Assert.Equal(1, articles.FindAll(article => article.Axe == "prévention").Count);
            //Assert.Equal(1, articles.FindAll(article => article.Axe == "recherche").Count);
            //Assert.Equal(2, articles.FindAll(article => article.Axe == "formation").Count);
        }
    }
}
